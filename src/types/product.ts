export interface ProductEntry {
  brand: string
  material: string
  sku: string
}

export type ProductsData = ProductEntry[]

export interface ProductSelection {
  brand: string
  material: string
  sku: string
} 